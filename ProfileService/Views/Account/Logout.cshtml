@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["PageTitle"] = "Sign Out";
}

<div class="logout-container mt-5 text-center">
    <h3 class="logout-header">@ViewData["PageTitle"]</h3>

    <div class="user-info mt-4">
        <p><strong>Name:</strong> @ViewData["UserName"]</p>
        <p><strong>Email:</strong> @ViewData["UserEmail"]</p>
    </div>

    <div class="logout-actions mt-4">
        <button id="confirmLogout" class="btn btn-warning btn-lg">
            <i class="bi bi-box-arrow-right"></i> Sign Out
        </button>
    </div>
</div>

<!-- JavaScript for Logout Confirmation -->
<script>
    document.getElementById("confirmLogout").addEventListener("click", function () {
        if (confirm("Are you sure you want to sign out?")) {
            window.location.href = '@Url.Action("Logout", "Account")';
        }
    });
</script>
